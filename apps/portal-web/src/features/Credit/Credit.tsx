import { NavBar } from "@/components";
import { Header } from "./Sections/Headet";

export const Credit = () => {
    return (
        <div>
            <NavBar />
            <Header />
        </div>
    );
}