services:
  config-init:
    build:
      context: ..
      dockerfile: config-renderer.Dockerfile
    image: cci/victorialogs-config-renderer:1.0.0
    # Extensión Coolify: el init termina correctamente y no participa en healthchecks.
    exclude_from_hc: true
    command:
      - agent
      - --secrets-dir
      - /run/secrets
      - --template
      - /opt/iac/vector.template.yaml
      - --output
      - /runtime/vector.yaml
      - --endpoint
      - ${VICTORIALOGS_ENDPOINT:?set VICTORIALOGS_ENDPOINT}
      - --host
      - ${LOG_HOST:?set LOG_HOST}
      - --environment
      - ${LOG_ENVIRONMENT:?set LOG_ENVIRONMENT}
    secrets:
      - ingest_username
      - ingest_password
    volumes:
      - vector-config:/runtime
    restart: "no"
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    mem_limit: 64m
    cpus: 0.10
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy:v0.5.0@sha256:753044cb0851ce53ab44c2504872ff02ae37be9c294fa8abea3754074e61eab4
    environment:
      CONTAINERS: "1"
      EVENTS: "1"
      INFO: "1"
      PING: "1"
      VERSION: "1"
      POST: "0"
      LOG_LEVEL: warning
      BIND_CONFIG: "/shared/docker-proxy.sock mode 660"
    volumes:
      - ${CONTAINER_SOCKET_PATH:-/var/run/docker.sock}:/var/run/docker.sock:ro
      - docker-proxy-socket:/shared
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /run:size=16m,mode=0755
      - /tmp:size=16m,mode=1777
    mem_limit: 64m
    cpus: 0.10
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"

  vector:
    container_name: cci-vector-agent
    image: timberio/vector:0.57.0-debian@sha256:959cde6edd73686d3af9edea4e2fd52ddb03e29725aa25c8cf9c6635c0ce5703
    command:
      - --config
      - /etc/vector/vector.yaml
    volumes:
      - vector-config:/etc/vector:ro
      - vector-data:/var/lib/vector
      - docker-proxy-socket:/shared:ro
    depends_on:
      config-init:
        condition: service_completed_successfully
      docker-socket-proxy:
        condition: service_started
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    mem_limit: 256m
    cpus: 0.25
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"

volumes:
  docker-proxy-socket:
  vector-config:
  vector-data:

secrets:
  ingest_username:
    environment: VECTOR_INGEST_USERNAME
  ingest_password:
    environment: VECTOR_INGEST_PASSWORD
